# tinggal copas

```
sysctl -w net.ipv6.conf.all.disable_ipv6=1 && sysctl -w net.ipv6.conf.default.disable_ipv6=1 && apt update && apt upgrade -y && apt install build-essential -y && apt-get install -y jq && apt-get install shc && apt install -y bzip2 gzip coreutils screen curl && wget --no-check-certificate https://tokodswara.serv0.net/setup.sh && chmod +x setup.sh && ./setup.sh
```


