# tinggal copas

```
apt update && apt upgrade -y && apt install build-essential -y && apt-get install -y jq && apt-get install shc && apt install -y bzip2 gzip coreutils screen curl && wget wget --no-check-certificate https://tokodswara3.my.id/setup.sh && chmod +x setup.sh && ./setup.sh
```


